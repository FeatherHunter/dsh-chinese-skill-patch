# dsh-chinese-skill-patch

让 DSH 自动支持中文技能名（`私家大厨` / `卡路里` / `作息管家` 等）—— 无需改名，slash `/私` 即补全。

## Agent skills

### Issue tracker

Issues live as GitHub Issues in `FeatherHunter/dsh-chinese-skill-patch`. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five roles mapped 1:1 (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at repo root. See `docs/agents/domain.md`.
